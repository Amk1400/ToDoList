"""
TODO
define an validator which its input argument is min & max
all natural numbers in that range is valid
in all validators in this file, we validate, then
if it was valid return the choosn value else we raise exception

all menus that fetch option from user must use this validator
so needed to update all codes of cli layer
"""
